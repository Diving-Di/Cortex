# Cortex 实现与生产验收待办

> 状态：未完成、部分完成和待验证事项的唯一清单
> 更新日期：2026-07-27
> 当前发布结论：**不通过，禁止发布**

本文只记录尚未完成的工作和缺失的生产证据。已经落地的设计与代码基线见
[软件设计说明书](SDD.md)。

状态定义：

- **未实现**：仓库中没有对应能力，或现有实现无法完成声明行为。
- **部分实现**：主链路存在，但分层、安全、恢复或用户操作不完整。
- **待验证**：已有代码或配置，但缺少可复现的端到端、隔离、性能或生产环境证据。
- **非 MVP**：明确不在当前范围，不作为发布阻断。

## 1. 发布阻断项

### P0：配置真实生成模型

知识问答和现有 AI 工作流需要有效 LiteLLM virtual key 及至少一个可用生成模型。
验收不得输出 Key，并需分别验证生成供应商、Ollama 和 reranker 不可用时的脱敏错误与降级。

### P0：完成知识库端到端和双租户验收

- 两个租户分别上传相同文件名、相同摘要和语义相似内容。
- 列表、详情、下载、删除、FTS、向量、rerank、Chat 和引用均不得跨租户。
- TXT、文本 PDF、DOCX 完成上传—解析—索引—检索—问答—删除。
- 删除后立即不再召回，旧 worker 不得重新激活文档。

### P1：完成检索质量和性能门槛

建立固定评测集并记录：

- Recall@8。
- MRR/nDCG。
- citation precision。
- 无答案准确率。
- P95 检索和端到端延迟。

## 2. 确认未实现

### 2.1 数据库和领域分层

- 将 6 张知识表、RLS、Policy、索引、授权和约束同步到 `backend/db/schema.sql` 空库基线。
- 更新空库表数量、RLS Policy 数量和 vector 扩展断言。
- 为 `000002_knowledge_base` 增加真实数据库 up/down、回滚后重放和重复执行测试。
- 增加统一知识领域 DTO。
- 将独立 Retriever、索引编排和 Knowledge Chat Workflow 从 handler/store 细节中分离。
- handler 只保留 HTTP/SSE 契约，不直接创建模型客户端、拼 Prompt 或编排 SQL。

### 2.2 测试资料与评测工具

- 在 `backend/testdata/knowledge/` 建立无真实个人数据的 TXT/PDF/DOCX 样本。
- 在 `backend/testdata/rag/` 建立问题、正确文档、正确片段和无答案样本。
- 建立可重复运行的检索质量与延迟评测工具。
- 增加知识库和成长助手页面测试。

### 2.3 AI 工作流迁移至 Eino

- 采用 CloudWeGo Eino 作为 Go AI 工作流框架，通过 OpenAI 兼容组件继续访问
  LiteLLM，不得绕过网关直连模型供应商。
- 保留现有 `AIClient`、`AIWorkflow` 和 `Retriever` 业务接口，新增 Eino
  适配实现，并以配置开关支持新旧实现并行验证和安全回滚。
- 按“笔记整理 → 报告生成 → 回忆/知识问答”的顺序渐进迁移，不进行一次性重写。
- 使用 Eino Graph/Chain 编排 Prompt、检索、结构化输出和模型流；handler 仍只处理
  HTTP/SSE 契约，不直接创建 Eino 组件或拼装工作流。
- Principal、租户解析、RLS、显式 `tenant_id` 条件、配额、审计、revision、
  草稿确认写入、来源归属和引用校验继续保留在可信业务层，不下放给框架或模型。
- 封装 Eino 与现有 SSE 事件的转换，保持公开 API、稳定错误码、客户端断线取消和
  “已输出后不从头重试”的行为兼容。
- 禁止将邮箱、姓名、完整正文、供应商 Key 或上游响应正文写入 Eino callback、
  trace、日志或缓存；Prompt/响应缓存保持默认关闭且不得跨租户共享。
- 为新旧实现增加契约对照测试，覆盖正常流式响应、结构化输出失败、超时、取消、
  LiteLLM 不可用、无来源报告、无证据回忆、跨租户资源伪造和回滚切换。
- 在完成基准测试并证明功能、隔离、错误契约和延迟无不可接受回退后，再移除旧实现；
  同步更新 `backend/README.md`、API/设计文档及 AI 验收脚本。

## 3. 部分实现、仍需补全

> 2026-07-27 代码补全记录：本轮已落地 `/api/v1` 会话 CRUD、三类来源范围、统一来源
> DTO、`request_id` 幂等与完整知识 SSE；知识库的树形筛选、搜索、状态、分页、详情、
> 预览、鉴权下载、重试/重建索引；成长助手的会话、来源多选、安全 Markdown、引用失效
> 降级与响应式 UI；PDF 重复页眉页脚和跨页续接、表格重复表头切块、相邻 parent 扩展、
> tombstone 持久重试、知识运行指标、Cortex 浏览器/OCI 标识和技术标识兼容策略。
> 下列需要真实 LiteLLM/Ollama、固定 reranker、多进程数据库竞争、容器重建或生产数据卷
> 的条目继续作为环境验收清单，不以本地单元测试代替生产证据。

### 3.1 Knowledge Chat 契约

当前已有知识 Chat、集合/文件范围限制、对话与消息保存、来源归属复核、引用持久化、
无证据拒答和基础 SSE 流。仍需补全：

- 提供 `/api/v1` 会话列表、新建、读取和删除接口。
- 在 API 和检索编排中真正支持 `knowledge`、`growth`、`all` 三种来源范围。
- 增加知识集合、知识文件和成长记录的统一来源 DTO。
- 增加 `request_id` 消息幂等，避免重试产生重复消息。
- 输出完整 SSE `retrieval`、`delta`、`sources`、`error`、`done` 事件。
- 增加文档提示注入、无证据、范围越权、来源失效和客户端断线测试。

### 3.2 知识库前端

当前已有集合筛选、文件上传、状态和失败消息、文件数量、删除确认以及基础加载和
空状态。仍需补全：

- 集合树、文件名搜索、状态筛选和服务端分页。
- 文件详情抽屉、提取预览和脱敏失败原因。
- 接入已有后端鉴权下载，并增加失败重试和重新索引。
- 非空集合删除时的 `COLLECTION_NOT_EMPTY` 指引。
- 失败、无权限、索引中、响应式和可访问性测试。

### 3.3 成长助手前端

当前已有基础问答、SSE 内容读取、停止请求、空状态和失败提示。仍需补全：

- 会话列表、新建、打开和删除。
- 知识库、成长记录和全部来源选择。
- 集合/文件多选及未 ready 文件排除提示。
- 安全 Markdown 渲染。
- 引用卡片、详情跳转、页码/标题/片段和已删除来源状态。
- SSE 事件、来源、断线、停止、无证据、响应式和可访问性测试。

### 3.4 文件解析

当前已有 TXT/PDF/DOCX 入口、PDF 页数/字符数/超时限制、加密和 OCR 错误分类、
DOCX 标题/列表/表格提取，以及基础签名、Zip Slip 和解压规模测试。仍需补全：

- 增加真实中文、英文和中英混合 TXT/PDF/DOCX 自动化样本。
- 覆盖损坏、加密、扫描、复杂多栏 PDF、重复页眉页脚、跨页段落和表格。
- 证明 PDF 页数、字符数、对象数量、解析时间和资源上限有效。
- 确认所有失败返回稳定错误码，日志不含正文、原路径或文件名。

### 3.5 父子切块

当前已有父子切块、标题边界、Token 上限、中英文处理、父块前后关系持久化以及
DOCX 表格基础提取。仍需补全：

- 实现或验证 PDF 阅读顺序、跨页语义和重复页眉页脚处理。
- 表格按重复表头的行组切割。
- 命中父块边界时，按预算选择性加入相邻 parent。
- 扩充文档树、结构边界、表格和部分批次故障测试。

### 3.6 模型客户端与缓存

当前已有 embedding/rerank HTTP 客户端、批量、有限重试、数量、顺序和维度校验。
仍需补全：

- 经 LiteLLM 验证本地 `qwen3-embedding:0.6b` 单条、16 条批量、中文、英文和混合输入。
- 确认返回数量、顺序和 1024 维，覆盖超时、不可达、维度异常和部分批次失败。
- 实现同租户 `content_hash + embedding_model` 的安全复用，或明确不缓存。
- 证明不存在跨租户 embedding 内容缓存。
- 用固定评测集确认可选 Qwen3 reranker 对 nDCG/MRR 的实际收益。

### 3.7 索引 worker

当前已有有限租约 claim、失败重试、索引代次检查和旧代次清理。仍需补全：

- 增加多 worker、多 backend、租约过期、幂等和故障重试测试。
- 覆盖删除、重传和索引并发，确保旧任务不能重新激活失效文档。
- 验证旧代次清理和模型升级回填不会混用向量空间。

### 3.8 删除恢复

当前删除流程会先使文档停止检索、取消索引任务、失效引用、删除 chunks，并使用
tombstone 文件名清理磁盘。仍需补全：

- 为磁盘清理失败增加持久化可重试任务。
- 数据库步骤失败时恢复 tombstone 文件名。
- 保证 DELETE 幂等并增加并发删除测试。
- 历史 UI 将已删除引用显示为不可用，不允许下载或展示旧片段。
- 明确文件彻底删除和本地模型日志保留承诺。

### 3.9 检索与隔离

当前已有 FTS、pgvector 精确搜索、RRF、可选 reranker、embedding/reranker 降级、
显式租户条件、RLS、当前索引代次过滤和统一上下文预算。仍需补全：

- 增加相邻 parent 扩展。
- 增加相同文本、相似文本、相同文件名和相同摘要的双租户隔离测试。
- 验证 collection/document 伪造、缺失 RLS 上下文和低权限角色行为。
- 记录 FTS 降级、reranker 降级、统一 token 预算和旧代次排除的端到端证据。

### 3.10 Docker 与可观测性

- 固定 Ollama 模型 digest。
- 在运行环境验证 reranker 使用固定官方 revision、离线启动且模型目录不可写。
- 增加 warm-up、队列长度、失败数、处理时长和检索延迟指标，不记录正文。
- 验证容器重建后数据库、附件和知识文件仍存在。
- 验证 `db`、`llm-gateway` 和 `backend` healthy。

### 3.11 品牌与兼容

当前 PWA metadata 和应用导航已经使用 Cortex，但浏览器标题、部分技术标识和
兼容策略仍未完成：

- 将浏览器标题从 Diary Listener 更新为 Cortex，并检查容器 OCI label。
- 明确 Go module、前端 package、环境变量、数据库角色、数据目录和 localStorage
  key 的旧标识兼容策略。
- 如需重命名技术标识，提供迁移、数据卷兼容和回滚方案。

## 4. 生产复验步骤

解除全部 P0 阻断后，在隔离环境执行：

1. 拉取所有镜像并核对固定 digest。
2. 在宿主机预拉取 `qwen3-embedding:0.6b`，记录模型清单和磁盘占用。
3. 限制 Ollama 只监听受信地址，确认 Docker 私有网络可以访问。
4. `docker compose up -d --build` 并等待必需服务 healthy。
5. 检查 vector 扩展、知识表、强制 RLS、复合外键和低权限授权。
6. 经 LiteLLM 执行本地 Qwen 单条、批量和多语言 1024 维验收。
7. 注册两个租户并执行相同/相似资料的全链路隔离测试。
8. 上传 TXT、文本 PDF、DOCX 并等待索引 ready。
9. 验证 child 召回、parent 上下文、reranker、引用页码和无证据拒答。
10. 删除被引用文件，确认立即失效、引用片段清理和历史 UI 降级。
11. 分别中断 embedding、reranker 和生成模型，验证预期降级。
12. 验证数据库和数据卷在容器重建后持久化。
13. 运行 `backend/scripts/non_ai_smoke.ps1` 和 `backend/scripts/ai_acceptance.ps1`。
14. 执行固定评测集并记录 Recall@8、MRR/nDCG、citation precision、无答案准确率和 P95。

只有所有 P0 阻断解除、复验通过、质量门槛达标后，才可将发布结论改为“允许发布”。

## 5. 当前已有但仅待复验的证据

2026-07-27 本地复核记录显示：

- `go vet ./...`、`go test ./...` 和 `go build ./cmd/server` 通过。
- `npm run format:check`、`npm test` 和 `npm run build` 通过。
- 注入无生产意义的临时占位配置后，Compose 配置解析通过。
- 后端和前端生产镜像曾成功构建。

这些结果不证明真实 Secret、本地模型、双租户 RAG、PDF/DOCX、删除并发或生产持久化可用。

## 6. 非 MVP 与按阈值启用

以下内容暂不作为当前发布阻断：

- 扫描 PDF OCR、手写识别和图片语义检索。
- XLSX、PPTX、音频和视频。
- 外部网页抓取、云盘同步、浏览器插件和团队共享。
- 用户画像、模型训练或微调。
- Apache Tika；仅在真实样本证明 Poppler/标准库不达标时重新评估。
- HNSW；仅在精确向量扫描达到数据量或延迟阈值后，以独立迁移引入。
- 多集合关联；当前一个知识文件最多属于一个集合。
