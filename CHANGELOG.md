# 更新日志

## 2026-07-23：完成 M4 正式版增强

### 功能新增

* 新增日、周、月定时报告任务，支持 IANA 时区和可配置执行时间。
* 新增定时任务查看、启用、禁用、立即执行、失败重试及最近 50 次结果记录。
* 到期任务使用独立调度角色和 `FOR UPDATE SKIP LOCKED` 原子领取，避免多实例重复执行。
* 自动报告严格使用当前租户来源笔记；无来源或 AI 未配置时记录失败，不生成虚构内容。
* 自动覆盖已有周期报告前保存历史版本，并持久化报告来源引用。
* 周期报告页面新增定时任务配置、执行状态、错误详情及生成报告跳转。

### 数据与安全

* 新增 `scheduled_report_tasks` 和 `scheduled_report_runs` 数据表及 Alembic
  `0006_scheduled_reports` 迁移。
* 两张任务表启用并强制执行 PostgreSQL RLS；生成过程重新建立可信租户上下文。
* 支持通过 `SCHEDULED_REPORTS_ENABLED` 禁用内置调度器，并通过
  `SCHEDULED_REPORT_POLL_SECONDS` 调整轮询间隔。

### 测试与文档

* 新增定时任务时区和日/周运行时间计算测试；后端相关 7 项测试通过。
* 前端 Vitest、TypeScript 检查和 Webpack production build 通过。
* 完成 59.1 分钟 Docker 稳定性测试：后端内存变化 +0.19 MiB，PID 全程稳定，
  未发现 OOM、连接池耗尽、异常堆栈或持续线性资源增长。
* 完成语义向量检索和 Web 自动部署评估，当前均决定暂不引入，并记录重新评估条件。
* 更新 SDD，完成 DL-300、DL-301、DL-303、DL-304 和 DL-306。

## 2026-07-23：完成 M3 工作台、PWA 与 Web 发布

### 功能新增

* 工作台新增今日摘要、最近笔记、连续记录天数、活跃热力图、笔记与 AI 用量统计，以及待生成报告提示。
* 新增工作台统计 API，并按用户时区计算本地日期边界和连续记录天数。
* 新增 PWA manifest、应用图标、Service Worker、离线页和离线状态提示。
* 前端容器改用多阶段生产构建，由 Nginx 提供 SPA 静态资源并反向代理 API。

### 架构调整

* 项目发布范围调整为纯 Web/PWA，移除 Windows 启动器、Inno Setup 安装包、内置 Python/PostgreSQL 运行时和相关验收配置。
* Docker Compose 增加前后端健康依赖和独立应用数据卷，FastAPI 启动前统一执行 Alembic 迁移。
* 通用数据目录不再依赖 Windows `%LOCALAPPDATA%`，默认使用后端数据目录并支持 `DIARY_DATA_DIR` 覆盖。

### 修复

* 修复 AI 流式响应进入生成器后事务级 RLS 租户上下文失效，导致用量记录或回忆来源无法写入的问题。

### 测试与文档

* 工作台统计通过 PostgreSQL 集成测试；时区边界和连续天数具有自动化测试。
* PostgreSQL 16 空库迁移后端 17 项测试全部通过；前端 Prettier、Vitest、TypeScript 和 Webpack production build 通过。
* 通过生产 Nginx 入口验证四类笔记、默认个人租户、工作台、备份、PWA 深层路由、跨租户拒绝和 `tenant_id` 注入拒绝。
* 完成服务重启持久性、备份恢复、RLS、安全扫描和日志验收；100 次工作台请求 p95 为 36.46 ms。
* 更新 README、API 文档、工程基线和 SDD，将 M3 与全部 MVP 完成条件标记为已验收，并新增 Web 发布验收记录。

## 220b429 (2026-07-23)：完成 M2 AI 工作流集成验收

### 修复

* 为 `messages` 增加租户复合唯一约束，确保 `message_sources` 复合外键可在全新 PostgreSQL 数据库中成功迁移。
* 优化中文回忆问题的关键词拆分与二元词片召回，支持“杭州发生了什么”等自然语言查询。

### 测试与文档

* 新增 M2 PostgreSQL 全链路测试，覆盖 AI 整理确认、改写前版本、报告来源、无来源拦截、覆盖策略、回忆引用及跨租户隔离。
* Alembic 已验证升级到 `0005_m2_ai_notes (head)`，后端 14 项 PostgreSQL 测试全部通过。
* 更新 SDD，将 DL-220～224、DL-230～236、DL-250～257、MVP-07 和 MVP-08 标记为完成。

## 4d77336 (2026-07-23)：新增 AI 笔记工作流并统一代码格式

### 界面优化

* 工作台新增快速记录整理流程，支持预览、编辑并确认 AI 生成的结构化草稿。
* 周期报告页面支持预览来源、流式生成日/周/月报告和确认覆盖已有报告。
* 回忆书页面支持自然语言日期查询、流式回答和引用笔记跳转。

### 功能新增

* 新增快速记录整理、周期报告和回忆问答 API；AI 内容未经用户确认不会写入笔记。
* 新增报告来源与回忆回答引用的持久化和查询能力。
* 新增 `report_sources`、`message_sources` 数据表、RLS 策略及 Alembic `0005_m2_ai_notes` 迁移。
* 新增 M2 服务单元测试，并补充相关接口文档。

### 工程优化

* 前端引入 Prettier，统一使用 2 空格缩进和 100 字符行宽。
* 后端引入 Black，统一使用 4 空格缩进和 100 字符行宽。
* 统一整理前端 TypeScript、TSX、CSS、配置文件以及后端 Python 文件格式。

## d4d1461 (2026-07-23)：更新 SDD 实现进度记录

### 文档更新

* 在更新日志中补充 SDD 阶段实现、架构调整和测试覆盖情况。
* 记录模块化重构及 SDD 15.1～15.8 的完成状态。

## 60df873 (2026-07-23)：完成 SDD 15.5～15.8 模块

### 功能新增

* 新增标签管理及笔记标签关联能力。
* 新增附件上传、列表、鉴权下载和删除功能。
* 新增中文笔记搜索及按类型、日期和标签筛选能力。
* 新增 Markdown ZIP 导出、带校验清单的备份和空租户受控恢复功能。
* 新增 OpenAI 兼容异步客户端、AI Provider 配置、SSE 流式响应和用量记录。
* 笔记页面接入标签、附件和搜索入口。

### 测试与文档

* 新增 AI 客户端测试和内容、数据、AI 集成测试。
* 更新 SDD 完成状态和 API 文档。

## 54424f5 (2026-07-23)：建立模块化应用架构

### 架构调整

* 后端拆分为 `core`、`models`、`schemas`、`services`、`api` 和 `ai` 分层结构。
* 数据库运行时迁移到 PostgreSQL，并为应用与迁移配置独立角色。
* 引入 Alembic 迁移、个人租户、认证 Token、笔记 CRUD、历史版本和 RLS 基础设施。
* 前端引入 React Router、TanStack Query、路由懒加载和 Webpack 分包预算。

### 功能新增

* 新增个人空间摘要、软删除和恢复能力。
* 新增笔记列表、创建、编辑、删除及历史版本恢复功能。
* 新增受保护路由和工作台、笔记、报告、回忆、搜索、设置等页面入口。

### 测试与文档

* 新增认证安全、租户上下文、健康检查和 PostgreSQL 集成测试。
* 新增数据库迁移说明、工程基线和环境变量示例。

## 1ee9378 (2026-07-22)：新增 Diary Listener 重构设计说明书

### 文档更新

* 新增软件设计说明书（SDD）。
* 记录项目目标、系统架构、数据设计、接口、安全要求、实施计划和验收标准。

## 10e2782 (2026-06-14)：重构为 AI 聊天与轻日记应用

### 界面优化

* 将原图片管理界面重构为认证、AI 聊天和轻日记界面。
* 新增聊天消息展示、日记列表、图片上传和登录注册页面。

### 功能新增

* 新增 FastAPI 后端、用户认证、AI 对话和日记接口。
* 新增 SQLite 数据存储、密码与 Token 安全处理以及图片上传能力。
* 新增 DeepSeek/OpenAI 兼容 AI 配置，并在未配置密钥时提供本地模拟回复。
* 新增 Dockerfile、Docker Compose 和 API 文档。
* 前端构建工具由 Vite 调整为 Webpack。
